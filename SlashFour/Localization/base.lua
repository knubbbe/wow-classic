	--
	-- local locale = DataByLocale[GetLocale()] or DataByLocale["enGB"]
	--
	-- local dungeon = locale.dungeons[i]
	-- if dungeon.tags:ContainsString(chatMessageWords[j]) then
	--	  button:SetText(dungeon.name)
	-- end
	--
	-- contains any dungeonsTags = Dungeon Name
	-- contains any exclusionTags = ignored
	-- contains inclusionTags & no exclusionTags = Misc.
	-- contains
	--

LocalizedTagTemplates = {}
--print("Tags init")